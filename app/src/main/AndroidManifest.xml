<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.leo.projetandroid">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        android:configChanges="locale">

        <activity android:name=".Display.GameDisplay">
        </activity>

        <activity
            android:name=".Display.OptionDisplay"
            android:parentActivityName=".Display.IntroMenuDisplay" >
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value=".Display.IntroMenuDisplay" />
        </activity>

        <activity android:name=".Display.CharacterDisplay">
        </activity>

        <activity android:name=".Display.InventoryDisplay">
        </activity>

        <activity android:name=".Display.MapDisplay">
        </activity>

        <activity android:name=".Display.IntroMenuDisplay">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

        </activity>
    </application>

</manifest>